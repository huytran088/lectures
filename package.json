{
  "name": "spring2026-lectures",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "uv run python scripts/generate-content.py",
    "validate": "uv run python scripts/validate_site.py",
    "dev": "vite --host 127.0.0.1",
    "build": "npm run generate && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.0.3"
  }
}
